import { createMuiTheme } from '@material-ui/core/styles';

export const rcaMuiTheme = createMuiTheme({
    palette: {
        primary: {
            main: '#f9b000'
        },
        secondary: {
            main: '#113670'
        },
        error: {
            main: '#cd1618'
        }
    }
});
